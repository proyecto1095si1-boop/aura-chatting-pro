// Versión Web (mock) del hook global de anuncios intersticiales
// react-native-google-mobile-ads no está soportado en la web,
// por lo tanto, esta versión no hace absolutamente nada.

export function useGlobalInterstitial() {
  // No-op en la web
}
